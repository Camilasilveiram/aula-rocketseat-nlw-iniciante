
let meta = {
    value: 'ler um livro por mês',
    checked: false,
    log: (info) => {
        console.log(info)
    }
}

meta.log(meta.value)

// fuction // arrow fuction 
const criarMeta = () => {}

function criarMeta () {}